@model Ecommerce.Model.CoupanModel
@{
    ViewBag.Title = "Create";
    Layout = "~/Areas/Admin/Views/Shared/AdminMaster.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        @if (TempData["message"] != null)
        {
        <div class="panel panel-info" id="infoPannal">
            <div class="panel-heading">
                Info
            </div>
            <div class="panel-body">
                @Html.Raw(TempData["message"].ToString())
                </div>
            </div>
        }
        <div class="panel panel-default">
            <div class="panel-heading">
                Add new Coupan
            </div>
            <div class="panel-body">
                @using (Html.BeginForm("Save", "Coupan"))
                        {
                <div class="row">
                    <div class="col-lg-10">
                        <div class="form-group">
                            <label>Code</label>
                            @Html.TextBoxFor(m => m.code, new { @class = "form-control", maxlength = "100" })
                            @Html.ValidationMessageFor(m => m.code)
                        </div>
                        <div class="form-group">
                            <label>Discount in percent</label>
                            @Html.TextBoxFor(m => m.DiscountPercentage, new { @class = "form-control", maxlength = "2" })
                            @Html.ValidationMessageFor(m => m.DiscountPercentage)
                        </div>
                         <div class="form-group">
                            <label>Valid From (MM/DD/YYYY)</label>
                            @Html.TextBoxFor(m => m.fromDate, new { @class = "form-control"  })
                            @Html.ValidationMessageFor(m => m.DiscountPercentage)
                        </div>
                         <div class="form-group">
                            <label>Valid Upto (MM/DD/YYYY)</label>
                            @Html.TextBoxFor(m => m.toDate, new { @class = "form-control" })
                            @Html.ValidationMessageFor(m => m.DiscountPercentage)
                        </div>
                        <div class="form-group">
                        <input type="submit" value="Save" class="btn btn-primary"/>
                        </div>
                    </div>
                </div>
                    @Html.HiddenFor(m=>m.id)
                }
            </div>
        </div>
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        setTimeout(HideInfo, 3500);
    });
    function HideInfo() {
        $("#infoPannal").hide();
    }
</script>
    
